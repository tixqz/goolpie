host: "http://localhost/"
port: 8800
basepath: /v1
paths:
    - endpoint: "/test"
      method: "POST"
      request: "foo"
      responseCode: 200
      responseBody: "bar"
    - endpoint: "/other"
      method: "GET"
      responseCode: 404
      responseBody: "bar"
